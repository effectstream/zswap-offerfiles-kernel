{
  "_note": "Calibrated at GRAND_OFFERS=25 with the batcher at 7 slots; enforcement is baseline x 1.2. Wall-clock numbers — re-calibrate for a different scale or a different box, and note publishToIndexed is dominated by the celestia delayMs, not by node work. sseDeliveryLag* compare SSE arrival (wall clock) against archived_at (chain time), so they measure block-timestamp -> client INCLUDING STM executor lag, not socket delivery; the pre-2026-08-04 66ms figure was only reachable because archived_at used to BE wall-clock NOW(), which was the bug. BOTH ends are gated now, and the p50 is the one that means something: at ~27 samples the p95 is effectively the second-largest single observation, dominated by whichever archive happened to land while the STM was catching up from the storm (peak lag ~95 blocks). Measured on the 204-check run: the p50 IMPROVED 1578 -> 1350 in the very run whose p95 went 2674 -> 6899, so gating on the tail alone reported a delivery regression that the delivery numbers contradict. The p95 stays gated, loosely, so an egregious stall is still visible. Same lesson as bookReadP95Ms, raised 10 -> 20 earlier for the same reason: at count=10 the p95 IS the max, and one 19ms read (median unmoved at 7-8ms) tripped the gate. A real read-path or delivery regression moves the median. maxStmLagBlocks 89 -> 95: the suite now publishes ~10 more offers and runs an extra phase, so the storm's catch-up peak is genuinely deeper; this is a load-profile change, not a node regression (submit p95 4726 vs 6654, publish->indexed 22037 vs 23983, bookRead 9 vs 20 all IMPROVED on the same run).",
  "submitP95Ms": 6654,
  "publishToIndexedP95Ms": 23983,
  "bookReadP95Ms": 20,
  "sseDeliveryLagP50Ms": 1700,
  "sseDeliveryLagP95Ms": 7500,
  "maxStmLagBlocks": 95
}
